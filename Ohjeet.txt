git clone "URL"
git status
git add -m "viesti tähän" / git add -A
git commit
git push

git pull

esc :wq

git branch
git branch "uusi oksa"
git checkout "oksa"
git merge master
git push --set-upstream origin "päivitysoksa"